<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jgh.myapplication">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.seoulactivity" />
        <activity android:name=".Activity.busanactivity" />
        <activity android:name=".Activity.chungbukactivity" />
        <activity android:name=".Activity.chungnamactivity" />
        <activity android:name=".Activity.daeguactivity" />
        <activity android:name=".Activity.daejeonactivity" />
        <activity android:name=".Activity.gangwonactivity" />
        <activity android:name=".Activity.gwangjuactivity" />
        <activity android:name=".Activity.gyungbukactivity" />
        <activity android:name=".Activity.gyunggiactivity" />
        <activity android:name=".Activity.gyungnamactivity" />
        <activity android:name=".Activity.inchunactivity" />
        <activity android:name=".Activity.jejuactivity" />
        <activity android:name=".Activity.jeonbukactivity" />
        <activity android:name=".Activity.jeonnamactivity" />
        <activity android:name=".Activity.ulsanactivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activity.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Activity.loding"
            android:theme="@style/Splash" />
        <activity android:name=".Activity.graph" />
        <activity android:name=".Activity.Graph3" />
        <activity android:name=".Activity.GraphAll" />
        <activity android:name=".Activity.DataAnalysisGraph" />
        <activity android:name=".Activity.CompareGraph"></activity>
    </application>

</manifest>